# @package _global_

# WebDataset dataloader, no compression.
# uv run src/train.py experiment=webdataset

defaults:
  - override /datamodule: webdataset
  - override /trainer: imagenet_vit_s_16
  - override /logger: wandb

seed: 42

experiment_name: "webdataset_baseline"
tags: ["benchmark", "webdataset"]

logger:
  wandb:
    offline: True
